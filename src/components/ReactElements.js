import React from 'react';

const ReactElements = () => {
  return (
    <div>
      <h1>React elements</h1>
      <p>Elements are the smallest building blocks of React apps.</p>
      <p>Elements are plain JavaScript objects.</p>
      <p>Elements are immutable.</p>
      <p>The most common way to create an element is via using JSX which is compiled to React.createElement() call.</p>
    </div>
  );
};

export default ReactElements;