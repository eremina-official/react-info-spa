import React from 'react';

const ReactJSX = () => {
  return (
    <div>
      <h1>JSX (JavaScript XML)</h1>
      <p>JSX is a JavaScript syntax extention.</p>
      <p>JSX is used to describe how the UI should look like.</p>
      <p>JSX is compiled to React.createElement() call that returns a React element. </p>
    </div>
  );
};

export default ReactJSX;