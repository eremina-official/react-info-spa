import React from 'react';

const ReactRouter = () => {
  return (
    <div>
      <h1>React Router</h1>
      <h2>This page provides a summary of React Router v4 features.</h2>
      <p><a href="https://github.com/ReactTraining/react-router">React Router</a> is a library for routing in React Applications.</p>
      <p>React Router composes routes in a declarative way.</p>
      <p>In its core React Router 4 is an API that is composed of React Components.</p>
      <p>The main React Router components are <code>BrowserRouter</code>, <code>Link</code> and <code>Route</code>.</p>
      <p><code>BrowserRouter</code> component wraps all the Route components.</p>
      <p><code>BrowserRouter</code> expects to receive a single child element.</p>
      <p><code>Link</code> components are used to generate links to the parts of dynamic content.</p>
      <p><code>Route</code> components</p>
    </div>
  );
};

export default ReactRouter;