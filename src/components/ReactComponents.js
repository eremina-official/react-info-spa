import React from 'react';

const ReactComponents = () => {
  return (
    <div>
      <h1>React components</h1>
      <h2>On this page the React Components main concepts are presented.</h2>
      <p>React Components are independent, reusable pieces of UI.</p>
      <p>Components return elements.</p>
      <p>Components are created using JavaScript function or by subclassing <code>React.Component</code> (Function and Class Components).</p>
      <p>The <code>render()</code> method is mandatory in a <code>React.Component</code> subclass.</p>
    </div>
  );
};

export default ReactComponents;